const mongoose = require('mongoose'); mongoose.connect('mongodb://127.0.0.1:27017/blood-donation-request').then(async () => { const Request = require('./src/models/Request'); const APIFeatures = require('./src/utils/APIFeatures'); const features = new APIFeatures(Request.find({status: {$ne: 'completed'}}), {}).filter().sort().limitFields().paginate(); const reqs = await features.query; console.log('Result length:', reqs.length); process.exit(0); });
