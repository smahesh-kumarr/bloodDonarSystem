const mongoose = require('mongoose'); mongoose.connect('mongodb://127.0.0.1:27017/blood-donation-request').then(async () => { const Request = require('./src/models/Request'); const APIFeatures = require('./src/utils/APIFeatures'); const features = new APIFeatures(Request.find({status: {$ne: 'completed'}}), {requesterId: '69a1f882d9686c18f88b640e'}).filter().sort().limitFields().paginate(); const reqs = await features.query; console.log(reqs.length); process.exit(0); });
